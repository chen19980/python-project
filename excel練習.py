import openpyxl as op
import openpyxl.styles as st
from openpyxl.styles import Font

# ex1 = op.Workbook()               #創建一個活頁簿
# wb = op.load_workbook("0414.xlsx")  #活頁簿
# ws = wb.active                      #工作表
 
# print(ws["A2"].value)                                 #工作表內的值  
# wb.create_sheet("hello")                              #新增工作表
# del wb["hello"]                                       #刪除工作表
# ws["A1"] = "hello world"                              #更改儲存格的值
# ws.cell(1,2,"ans")                                    #更改儲存格的值 (row,column,值)
# ws["B1"].font = Font(bold=True, color="000000FF")     #更改字體
# ws.append(["happy"])                                  #加入值

# a = [1,3]
# b = [4,5]
# y = list(zip(a,b))
# for x in y:
#     ws.append(x)

# wb.save("0414.xlsx")
















